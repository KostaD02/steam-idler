export interface SteamAccountProfile {
  /** Steam profile display name (persona `player_name`). */
  name: string;
  /** Full-size Steam avatar URL (persona `avatar_url_full`). */
  avatarUrl: string;
}
