# Angular runtime config.
# At container start, `envsubst` replaces ${VARS} in apps/client/config.template.json
# and writes the result to the served /config.json. Copy to `apps/client/.env` for local docker runs.

API_BASE=http://localhost:3000/api
